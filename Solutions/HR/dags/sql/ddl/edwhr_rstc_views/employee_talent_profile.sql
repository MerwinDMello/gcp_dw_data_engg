/*==============================================================*/
/* Table: Employee_Talent_Profile                               */
/*==============================================================*/
/***************************************************************************************
   ******************   E D W H R    R S T C    V I E W   ******************
****************************************************************************************/
CREATE OR REPLACE VIEW {{params.param_hr_rstc_views_dataset_name}}.employee_talent_profile AS SELECT
    employee_talent_profile.employee_talent_profile_sid,
    employee_talent_profile.valid_from_date,
    employee_talent_profile.employee_sid,
    employee_talent_profile.employee_first_name,
    employee_talent_profile.employee_middle_name,
    employee_talent_profile.employee_last_name,
    employee_talent_profile.employee_num,
    employee_talent_profile.employee_3_4_login_code,
    employee_talent_profile.job_family_text,
    employee_talent_profile.job_code,
    employee_talent_profile.job_code_sid,
    employee_talent_profile.position_code_text,
    employee_talent_profile.position_code,
    employee_talent_profile.position_sid,
    employee_talent_profile.position_title_text,
    employee_talent_profile.org_hierarchy_text,
    employee_talent_profile.manager_name,
    employee_talent_profile.manager_employee_num,
    employee_talent_profile.travel_willingness_id,
    employee_talent_profile.travel_willingness_pct_range_text,
    employee_talent_profile.travel_location_text,
    employee_talent_profile.relocation_willingness_id,
    employee_talent_profile.relocation_location_text,
    employee_talent_profile.calibrated_performance_rating_id,
    employee_talent_profile.overall_performance_rating_id,
    employee_talent_profile.smry_competency_rating_id,
    employee_talent_profile.smry_goal_rating_id,
    employee_talent_profile.employee_promotability_interest_id,
    employee_talent_profile.potential_performance_id,
    employee_talent_profile.future_role_1_leadership_level_id,
    employee_talent_profile.future_role_1_org_size_id,
    employee_talent_profile.future_role_1_timeframe_id,
    employee_talent_profile.future_role_1_role_id,
    employee_talent_profile.future_role_2_leadership_level_id,
    employee_talent_profile.future_role_2_org_size_id,
    employee_talent_profile.future_role_2_timeframe_id,
    employee_talent_profile.future_role_2_role_id,
    employee_talent_profile.flight_risk_probability_id,
    employee_talent_profile.flight_risk_timeframe_id,
    employee_talent_profile.flight_risk_driver_text,
    employee_talent_profile.flight_risk_secondary_driver_text,
    employee_talent_profile.valid_to_date,
    employee_talent_profile.lawson_company_num,
    employee_talent_profile.process_level_code,
    employee_talent_profile.calibration_session_created_date,
    employee_talent_profile.calibration_session_name,
    employee_talent_profile.calibration_session_last_mod_date_time,
    employee_talent_profile.calibration_session_published_date,
    employee_talent_profile.calibration_session_created_by_name,
    employee_talent_profile.calibration_box_id,
    employee_talent_profile.company_tenure_text,
    employee_talent_profile.position_tenure_text,
    employee_talent_profile.source_system_code,
    employee_talent_profile.dw_last_update_date_time
  FROM
    {{params.param_hr_base_views_dataset_name}}.employee_talent_profile
;
