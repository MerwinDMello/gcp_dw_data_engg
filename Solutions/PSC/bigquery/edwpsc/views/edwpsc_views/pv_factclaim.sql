CREATE OR REPLACE VIEW edwpsc_views.`pv_factclaim`
AS SELECT
  `pv_factclaim`.claimkey,
  `pv_factclaim`.claimnumber,
  `pv_factclaim`.coid,
  `pv_factclaim`.regionkey,
  `pv_factclaim`.claimdatekey,
  `pv_factclaim`.servicedatekey,
  `pv_factclaim`.patientkey,
  `pv_factclaim`.guarantorpatientkey,
  `pv_factclaim`.servicingproviderkey,
  `pv_factclaim`.renderingproviderkey,
  `pv_factclaim`.paytoproviderkey,
  `pv_factclaim`.supervisorproviderkey,
  `pv_factclaim`.addprovider1providerkey,
  `pv_factclaim`.addprovider2providerkey,
  `pv_factclaim`.liabilitydatekey,
  `pv_factclaim`.billedtoid,
  `pv_factclaim`.billedtotype,
  `pv_factclaim`.facilitykey,
  `pv_factclaim`.claimstatuskey,
  `pv_factclaim`.encounterkey,
  `pv_factclaim`.encounterid,
  `pv_factclaim`.totalchargesamt,
  `pv_factclaim`.totalbalanceamt,
  `pv_factclaim`.totalpatientbalanceamt,
  `pv_factclaim`.totalinsurancebalanceamt,
  `pv_factclaim`.totalpatientbalanceminuspatientiplansamt,
  `pv_factclaim`.totalpaymentsamt,
  `pv_factclaim`.totalinsurancepaymentsamt,
  `pv_factclaim`.totalpatientpaymentsamt,
  `pv_factclaim`.totaladjustmentsamt,
  `pv_factclaim`.totaluncoveredamt,
  `pv_factclaim`.totalallowedamt,
  `pv_factclaim`.totalpatientresponsibilityamt,
  `pv_factclaim`.totalestimatedpatientbalanceamt,
  `pv_factclaim`.totalpaymentsandadjustmentsamt,
  `pv_factclaim`.voidflag,
  `pv_factclaim`.deleteflag,
  `pv_factclaim`.coidconfigurationkey,
  `pv_factclaim`.claimcreatedbyuserkey,
  `pv_factclaim`.claimqueueid,
  `pv_factclaim`.referringproviderkey,
  `pv_factclaim`.referringproviderid,
  `pv_factclaim`.referringprovidertype,
  `pv_factclaim`.copay,
  `pv_factclaim`.arclaimflag,
  `pv_factclaim`.payer1iplankey,
  `pv_factclaim`.voidedclaimreversedclaimnumber,
  `pv_factclaim`.voidedclaimcopyclaimnumber,
  `pv_factclaim`.voidedclaimoriginalclaimnumber,
  `pv_factclaim`.sourceaprimarykeyvalue,
  `pv_factclaim`.sourcearecordlastupdated,
  `pv_factclaim`.sourceachangedflag,
  `pv_factclaim`.dwlastupdatedatetime,
  `pv_factclaim`.sourcesystemcode,
  `pv_factclaim`.insertedby,
  `pv_factclaim`.inserteddtm,
  `pv_factclaim`.modifiedby,
  `pv_factclaim`.modifieddtm,
  `pv_factclaim`.claimlock,
  `pv_factclaim`.claimtypeflag,
  `pv_factclaim`.accidentindflag,
  `pv_factclaim`.accidentstate,
  `pv_factclaim`.accidenthour,
  `pv_factclaim`.accidentrel,
  `pv_factclaim`.symindflag,
  `pv_factclaim`.symdatekey,
  `pv_factclaim`.emprelated,
  `pv_factclaim`.admitdatekey,
  `pv_factclaim`.dischargedatekey,
  `pv_factclaim`.splpgmid,
  `pv_factclaim`.epsdtreferral,
  `pv_factclaim`.epsdtrefsts,
  `pv_factclaim`.crimerelatedflag,
  `pv_factclaim`.docsentdatekey,
  `pv_factclaim`.pwk06,
  `pv_factclaim`.pregindctorflag,
  `pv_factclaim`.relinfodatekey,
  `pv_factclaim`.relinfoind,
  `pv_factclaim`.labchargesamt,
  `pv_factclaim`.labindflag,
  `pv_factclaim`.patientid,
  `pv_factclaim`.servicingproviderid,
  `pv_factclaim`.renderingproviderid,
  `pv_factclaim`.paytoproviderid,
  `pv_factclaim`.supervisorproviderid,
  `pv_factclaim`.addprovider1providerid,
  `pv_factclaim`.addprovider2providerid,
  `pv_factclaim`.facilityid,
  `pv_factclaim`.practiceid,
  `pv_factclaim`.claimcreatedbyid,
  `pv_factclaim`.deptcode,
  `pv_factclaim`.payer1iplanid,
  `pv_factclaim`.payer1financialclasskey,
  `pv_factclaim`.payer2iplankey,
  `pv_factclaim`.payer2iplanid,
  `pv_factclaim`.payer2financialclasskey,
  `pv_factclaim`.payer3iplankey,
  `pv_factclaim`.payer3iplanid,
  `pv_factclaim`.payer3financialclasskey,
  `pv_factclaim`.collectionstatus,
  `pv_factclaim`.collectioncodechangedate,
  `pv_factclaim`.collectioncode,
  `pv_factclaim`.collectioncodechangedatedate,
  `pv_factclaim`.collectionstatusaging,
  `pv_factclaim`.claimpractice,
  `pv_factclaim`.claimbilledflag,
  `pv_factclaim`.claimlogbookid,
  `pv_factclaim`.linebalanceamt,
  `pv_factclaim`.sourcebprimarykeyvalue,
  `pv_factclaim`.holdcode,
  `pv_factclaim`.payer1pvfinancialclass,
  `pv_factclaim`.payer1pvfinancialclassdesc,
  `pv_factclaim`.payer1pvfinancialclassgrouped,
  `pv_factclaim`.liabilityiplankey,
  `pv_factclaim`.relayhealthflag,
  `pv_factclaim`.protocolname,
  `pv_factclaim`.sysstarttime,
  `pv_factclaim`.sysendtime
  FROM
    edwpsc_base_views.`pv_factclaim`
  INNER JOIN edwpsc_base_views.secref_facility
  ON LPAD(TRIM(secref_facility.co_id, ' '), 5, '0') = LPAD(TRIM(`pv_factclaim`.coid, ' '), 5, '0')
  AND TRIM(secref_facility.user_id, ' ') = TRIM(session_user(), ' ')
;