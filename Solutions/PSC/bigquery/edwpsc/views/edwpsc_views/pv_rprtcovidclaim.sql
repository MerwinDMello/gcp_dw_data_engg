CREATE OR REPLACE VIEW edwpsc_views.`pv_rprtcovidclaim`
AS SELECT
  `pv_rprtcovidclaim`.covidclaimkey,
  `pv_rprtcovidclaim`.claimkey,
  `pv_rprtcovidclaim`.regionkey,
  `pv_rprtcovidclaim`.coid,
  `pv_rprtcovidclaim`.dateofservicekey,
  `pv_rprtcovidclaim`.dateofservicemonthendkey,
  `pv_rprtcovidclaim`.claimdatekey,
  `pv_rprtcovidclaim`.patientaccountnum,
  `pv_rprtcovidclaim`.covidclaimnumber,
  `pv_rprtcovidclaim`.providernpi,
  `pv_rprtcovidclaim`.groupnpi,
  `pv_rprtcovidclaim`.grouptaxid,
  `pv_rprtcovidclaim`.covidchargesamt,
  `pv_rprtcovidclaim`.covidbalanceamt,
  `pv_rprtcovidclaim`.covidpatbalanceamt,
  `pv_rprtcovidclaim`.covidinsbalanceamt,
  `pv_rprtcovidclaim`.covidinspaymentamt,
  `pv_rprtcovidclaim`.covidpatpaymentamt,
  `pv_rprtcovidclaim`.covidrefundsamt,
  `pv_rprtcovidclaim`.accounttotalbalanceamt,
  `pv_rprtcovidclaim`.accountpatientbalanceamt,
  `pv_rprtcovidclaim`.accountinsurancebalanceamt,
  `pv_rprtcovidclaim`.accountnonzerobalclaimscnt,
  `pv_rprtcovidclaim`.payer1iplanname,
  `pv_rprtcovidclaim`.payer1ifinancialclassname,
  `pv_rprtcovidclaim`.payer2iplanname,
  `pv_rprtcovidclaim`.payer3iplanname,
  `pv_rprtcovidclaim`.liabilityowner,
  `pv_rprtcovidclaim`.liabilityfinancialclassname,
  `pv_rprtcovidclaim`.practiceid,
  `pv_rprtcovidclaim`.patientcategory,
  `pv_rprtcovidclaim`.collectioncode,
  `pv_rprtcovidclaim`.firstdenialcategory,
  `pv_rprtcovidclaim`.coviddxprimary,
  `pv_rprtcovidclaim`.primarydx,
  `pv_rprtcovidclaim`.billableflag,
  `pv_rprtcovidclaim`.firstpatientstatementdatekey,
  `pv_rprtcovidclaim`.poskey,
  `pv_rprtcovidclaim`.dxb342flag,
  `pv_rprtcovidclaim`.cpid,
  `pv_rprtcovidclaim`.innetworkflag,
  `pv_rprtcovidclaim`.providername,
  `pv_rprtcovidclaim`.patientbalanceflag,
  `pv_rprtcovidclaim`.netpatientpaymentamt,
  `pv_rprtcovidclaim`.netpatientpaymentflag,
  `pv_rprtcovidclaim`.hrsapatientflag,
  `pv_rprtcovidclaim`.refunddate,
  `pv_rprtcovidclaim`.patientfirstname,
  `pv_rprtcovidclaim`.middleinitial,
  `pv_rprtcovidclaim`.patientlastname,
  `pv_rprtcovidclaim`.dobkey,
  `pv_rprtcovidclaim`.servicetype,
  `pv_rprtcovidclaim`.typeofid,
  `pv_rprtcovidclaim`.idnumber,
  `pv_rprtcovidclaim`.patientgender,
  `pv_rprtcovidclaim`.patientprimarystatekey,
  `pv_rprtcovidclaim`.patientprimaryaddressline1,
  `pv_rprtcovidclaim`.patientprimaryaddressline2,
  `pv_rprtcovidclaim`.patientprimarycity,
  `pv_rprtcovidclaim`.patientprimaryzipcode,
  `pv_rprtcovidclaim`.totaladjustmentscharityamt,
  `pv_rprtcovidclaim`.totaladjustmentsuninsuredamt,
  `pv_rprtcovidclaim`.firstinsbilldatekey,
  `pv_rprtcovidclaim`.firstpatbilldatekey,
  `pv_rprtcovidclaim`.inssubmitted,
  `pv_rprtcovidclaim`.patientstatementsent,
  `pv_rprtcovidclaim`.payer2ifinancialclassname,
  `pv_rprtcovidclaim`.payer3ifinancialclassname,
  `pv_rprtcovidclaim`.cpt99000flag,
  `pv_rprtcovidclaim`.covidtestflag,
  `pv_rprtcovidclaim`.cpt87635flag,
  `pv_rprtcovidclaim`.csmodifierflag,
  `pv_rprtcovidclaim`.csmodifieddatekey,
  `pv_rprtcovidclaim`.worklist,
  `pv_rprtcovidclaim`.emflag,
  `pv_rprtcovidclaim`.scodeflag,
  `pv_rprtcovidclaim`.voidedclaimoriginalclaimnumber,
  `pv_rprtcovidclaim`.patientmobilephone,
  `pv_rprtcovidclaim`.facilityname,
  `pv_rprtcovidclaim`.facilityprimaryaddressline1,
  `pv_rprtcovidclaim`.facilityprimaryaddressline2,
  `pv_rprtcovidclaim`.facilitycity,
  `pv_rprtcovidclaim`.facilitystate,
  `pv_rprtcovidclaim`.facilityzip,
  `pv_rprtcovidclaim`.cptcodes,
  `pv_rprtcovidclaim`.dxcodes,
  `pv_rprtcovidclaim`.rpartyname,
  `pv_rprtcovidclaim`.rpartyaddrline1,
  `pv_rprtcovidclaim`.rpartyaddrline2,
  `pv_rprtcovidclaim`.rpartycity,
  `pv_rprtcovidclaim`.rpartystate,
  `pv_rprtcovidclaim`.rpartyzip,
  `pv_rprtcovidclaim`.covidcreditbalanceflag,
  `pv_rprtcovidclaim`.claimstatusname,
  `pv_rprtcovidclaim`.daysfromservicedate,
  `pv_rprtcovidclaim`.agingbucket,
  `pv_rprtcovidclaim`.dwlastupdatedatetime,
  `pv_rprtcovidclaim`.sourcesystemcode,
  `pv_rprtcovidclaim`.patientrace,
  `pv_rprtcovidclaim`.patientethnicity,
  `pv_rprtcovidclaim`.totalfinancialadjustmentsamt,
  `pv_rprtcovidclaim`.rhlastreleasedate
  FROM
    edwpsc_base_views.`pv_rprtcovidclaim`
  INNER JOIN edwpsc_base_views.secref_facility
  ON LPAD(TRIM(secref_facility.co_id, ' '), 5, '0') = LPAD(TRIM(`pv_rprtcovidclaim`.coid, ' '), 5, '0')
  AND TRIM(secref_facility.user_id, ' ') = TRIM(session_user(), ' ')
;