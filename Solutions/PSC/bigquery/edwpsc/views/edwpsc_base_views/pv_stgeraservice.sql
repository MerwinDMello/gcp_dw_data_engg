CREATE OR REPLACE VIEW edwpsc_base_views.`pv_stgeraservice`
AS SELECT
  `pv_stgeraservice`.practice,
  `pv_stgeraservice`.era_date,
  `pv_stgeraservice`.era_num,
  `pv_stgeraservice`.st_ctrl_num,
  `pv_stgeraservice`.pvd_num,
  `pv_stgeraservice`.claim_num,
  `pv_stgeraservice`.svc_code,
  `pv_stgeraservice`.modifier,
  `pv_stgeraservice`.ctrl_num,
  `pv_stgeraservice`.line_num,
  `pv_stgeraservice`.claim_status,
  `pv_stgeraservice`.inv_num,
  `pv_stgeraservice`.proc_code,
  `pv_stgeraservice`.ar_ctrl_num,
  `pv_stgeraservice`.modifier1,
  `pv_stgeraservice`.modifier2,
  `pv_stgeraservice`.modifier3,
  `pv_stgeraservice`.svc_date,
  `pv_stgeraservice`.pos_code,
  `pv_stgeraservice`.rndr_pvd_id,
  `pv_stgeraservice`.status,
  `pv_stgeraservice`.charge,
  `pv_stgeraservice`.expected,
  `pv_stgeraservice`.covered,
  `pv_stgeraservice`.deductible,
  `pv_stgeraservice`.copay,
  `pv_stgeraservice`.paid_to_patient,
  `pv_stgeraservice`.payment,
  `pv_stgeraservice`.noncovered,
  `pv_stgeraservice`.payment_posted,
  `pv_stgeraservice`.adj_posted,
  `pv_stgeraservice`.deduct_posted,
  `pv_stgeraservice`.increased_ar,
  `pv_stgeraservice`.balance,
  `pv_stgeraservice`.reason,
  `pv_stgeraservice`.last_upd_userid,
  `pv_stgeraservice`.last_upd_datetime,
  `pv_stgeraservice`.eraservicepk,
  `pv_stgeraservice`.regionkey,
  `pv_stgeraservice`.sourcephysicaldeleteflag,
  `pv_stgeraservice`.dwlastupdatedatetime
  FROM
    edwpsc.`pv_stgeraservice`
;