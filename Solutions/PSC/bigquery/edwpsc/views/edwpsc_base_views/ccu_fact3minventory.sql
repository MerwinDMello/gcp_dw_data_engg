CREATE OR REPLACE VIEW edwpsc_base_views.`ccu_fact3minventory`
AS SELECT
  `ccu_fact3minventory`.ccu3minventorykey,
  `ccu_fact3minventory`.sourcesystem,
  `ccu_fact3minventory`.worklist,
  `ccu_fact3minventory`.owner,
  `ccu_fact3minventory`.regionkey,
  `ccu_fact3minventory`.coid,
  `ccu_fact3minventory`.ccugolivestatus,
  `ccu_fact3minventory`.ccugolivedate,
  `ccu_fact3minventory`.assignedtovendor,
  `ccu_fact3minventory`.vendorname,
  `ccu_fact3minventory`.vendorassigneddate,
  `ccu_fact3minventory`.locationkey,
  `ccu_fact3minventory`.practicekey,
  `ccu_fact3minventory`.facilitykey,
  `ccu_fact3minventory`.encounterid,
  `ccu_fact3minventory`.servicedate,
  `ccu_fact3minventory`.patientkey,
  `ccu_fact3minventory`.patientfirstname,
  `ccu_fact3minventory`.patientlastname,
  `ccu_fact3minventory`.patientmiddlename,
  `ccu_fact3minventory`.servicingproviderkey,
  `ccu_fact3minventory`.visitstatusdescription,
  `ccu_fact3minventory`.encounterlockeddate,
  `ccu_fact3minventory`.inventorytype,
  `ccu_fact3minventory`.claimvsencounterflag,
  `ccu_fact3minventory`.sourcelastupdateddatetime,
  `ccu_fact3minventory`.groupassignment,
  `ccu_fact3minventory`.servicedateconsolidationdateflag,
  `ccu_fact3minventory`.nonbillable,
  `ccu_fact3minventory`.accountid,
  `ccu_fact3minventory`.visittype,
  `ccu_fact3minventory`.dischargedate,
  `ccu_fact3minventory`.careproviderfirstname,
  `ccu_fact3minventory`.careproviderlastname,
  `ccu_fact3minventory`.careprovidermiddlename,
  `ccu_fact3minventory`.recordstatus,
  `ccu_fact3minventory`.docholdreason,
  `ccu_fact3minventory`.encounter3mkey,
  `ccu_fact3minventory`.loaddate,
  `ccu_fact3minventory`.worked,
  `ccu_fact3minventory`.workeddate,
  `ccu_fact3minventory`.encounterlastmodifieddate,
  `ccu_fact3minventory`.dayssincemodifieddate,
  `ccu_fact3minventory`.dayssinceservicedate,
  `ccu_fact3minventory`.busincessdayssinceservicedate,
  `ccu_fact3minventory`.busincessdayssincelastmodified,
  `ccu_fact3minventory`.procedurecategory1,
  `ccu_fact3minventory`.procedurecategory2,
  `ccu_fact3minventory`.cptcodes,
  `ccu_fact3minventory`.ccunoncustomerconfigkey,
  `ccu_fact3minventory`.ccunoncustomernpiconfigkey,
  `ccu_fact3minventory`.dwlastupdatedatetime,
  `ccu_fact3minventory`.insertedby,
  `ccu_fact3minventory`.inserteddtm,
  `ccu_fact3minventory`.modifiedby,
  `ccu_fact3minventory`.modifieddtm,
  `ccu_fact3minventory`.cptmodifiers,
  `ccu_fact3minventory`.icdcodes,
  `ccu_fact3minventory`.loadkey,
  `ccu_fact3minventory`.facilityid
  FROM
    edwpsc.`ccu_fact3minventory`
;