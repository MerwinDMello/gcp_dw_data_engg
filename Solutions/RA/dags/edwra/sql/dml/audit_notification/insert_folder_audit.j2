INSERT INTO {{ params.param_parallon_ra_core_dataset_name }}.folder_audit (
  ssc_name,
  file_type,
  source_folder_path, 
  folder_file_count,
  copied_date_time
) 
VALUES 
  (
    '{{ operational_group }}',
    '{{ file_name_mnemonic }}',
    '{{ source_folder_path }}', 
	  {{ folder_file_count }}, 
	  datetime_trunc(current_datetime('US/Central'), SECOND)
  );