INSERT INTO {{ params.param_parallon_ra_core_dataset_name }}.excel_audit (
  ssc_name,
  file_type,
  file_name, 
  file_count, 
  table_count, 
  load_date
  ) 
VALUES 
  (
    '{{ operational_group }}',
    '{{ file_name_mnemonic }}',
    '{{ file_name }}', 
	  {{ all_src_rec_count }}, 
    {{ target_load_count }}, 
	  datetime_trunc(current_datetime('US/Central'), SECOND)
  );